name = input("Enter you full name")

# result =  len(name)
#result =  name.find("Z") # used to find the first occurence of anything

# result =  name.rfind("a")  # rfind reverse find last occurrence of anything in the stiring 

# name =  name.capitalize() # capitalizes only the first letter of the string
# name =  name.upper()
# name = name.lower()
# result = name.isdigit() # true only for digits
# result = name.isalpha() # true only for string without spaces
# name = name.count("4") # counts the no. of anything in the string
name = name.replace("a", "aa")

print(name)