# calc circumference of a circle

import math

'''
radius = float(input("Enter the radius of a circle: "))

circumference = 2 * math.pi * radius
print(f"The radius of circle is: {round(circumference, 2)}cm")


# Area of circle

area = math.pi * pow(radius, 2)

print(f"Area of the cicle is {round(area, 2)}cm²")

'''

# calc the hypotenuse of triangle

a = float(input("Enter side A: "))
b = float(input("Enter side B: "))

c = math.sqrt(pow(a, 2) + pow(b, 2))

print(f"The length of hypotenuse is: {c} cm")